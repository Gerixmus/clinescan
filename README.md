# CLINESCAN

CLINESCAN (C-Line-Scan) is a model based on CodeBERT for detecting vulnerable lines in C/C++ code. 

It is trained on the Big-Vul dataset for binary classification at the line level (vulnerable vs non-vulnerable).
